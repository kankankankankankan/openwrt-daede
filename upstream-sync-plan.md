# openwrt-daede 上游自动同步方案

日期：2026-09-22。FF-110 本轮方案交付；尚未实施或启用自动化。

推荐采用「保留 Git 合并历史 + 缩小定制改动范围 + 自动同步 PR + 验证后自动合并」。正常兼容更新无需人工操作；未知冲突和验证失败自动停止，保留当前可用版本。任意上游变更都保证正确且完全零人工不可承诺。

## 1. 已核实的现状

| 项目 | 本轮证据 |
| --- | --- |
| 自有仓库 | `kankankankankankan/openwrt-daede`，远端 main 为 `ad3ed693e66e3cd070562f4d5e55a38bfafb275c` |
| 整包上游 | `kenzok8/openwrt-daede`，远端 main 为 `91fa0c7da6d63b1654ebf1e615dbc2a6498216a6` |
| 共同祖先 | `051b25455428921512f0eac67772f2a01af317b3`；已有合并历史，可以直接继续三方合并 |
| 待同步更新 | 上游有 8 个未合入提交，涉及构建、源码资产、核心兼容补丁和日志控件 |
| 合并预演 | `git merge-tree --write-tree origin/main upstream/main` 返回 1：`luci-app-daede/Makefile` 内容冲突；`luci-app-daede/po/zh_Hans/daede.po` 修改/删除冲突 |
| 当前自动化 | `auto-bump.yml` 跟踪底层 dae/daed/dae-wing 等依赖、暂存构建后直推 main；不负责整包上游同步 |
| GitHub 设置 | `allow_auto_merge=false`；main 分支保护接口返回未保护，仓库 rulesets 列表为空 |

本任务工作分支为 `agent/agent/ff-110`，HEAD `583382a`；与 `origin/main` 的树差异仅为历史截图附件。本轮以远端 main 为方案基线，读取了关键工作流、版本文件、会员模块接入点和现有测试入口。拉取上游并预演合并未切换分支、未修改产品文件，也未生成实际合并提交。

## 2. 同步来源与分支规则

- 整包更新只跟 `kenzok8/openwrt-daede/main`。其锁定的 `ci/pins.env`、核心补丁及 dae/daed Makefile 作为一组更新，避免混配。
- 第一阶段让当前 fork 的底层 `auto-bump.yml` 处于显式禁用状态，禁止与整包同步同时直写 main。禁用应由本仓库策略控制，即使合入上游更新也不能重新开启。需要早于 kenzok8 跟进核心时，再增加独立依赖 PR 通道，共用同一合并门槛。
- `main` 保存已验证的定制版。`sync/upstream` 是机器人唯一管理的候选分支，每次从最新 main 合入固定的上游 SHA；同一时刻最多一个同步 PR。
- 同步 PR 必须使用 **merge commit**。不能 squash 或 rebase 掉引入的上游祖先关系，否则下次同步可能反复遇到旧改动。无需增加长期镜像分支。
- 不用 `reset --hard upstream/main`、`gh repo sync --force` 覆盖定制版，不对整个业务文件使用 `ours`/`theirs`。

## 3. 降低冲突的代码组织

保留当前已拆出的 `member.js`、`lan-interface.js`、`member-sync.sh`、`member-sync-cron.sh`。逐步把会员入口、样式和安装清单集中到定制模块/少量稳定接入点；`dae.js`、公共样式、UCI、ACL、Makefile 只保留必要接入。先验证原有行为，再做小步提取，不为自动同步先重写整个项目。

新增本仓库维护的策略文件（建议 `ci/downstream-policy.json`），描述目标架构、版本规则、明确删除的文件、发布目标和禁用的上游维护任务。策略由本仓库维护，不能由待合入代码自行放宽；修改策略本身需要一次审查。

针对已发现的两个冲突：

1. **包版本号**：只在冲突区域确认为 `PKG_RELEASE` 及约定注释时自动处理，其他安装项和依赖项仍做三方合并。同一 `PKG_VERSION` 下，下游 release 取 `max(当前下游, 上游)+1`，本次即 `1.15-r9`。上游版本升级时按新版本系列计算，并用 opkg/apk 的实际版本比较规则确认不会降级；版本倒退则停止。以 base/upstream SHA 组合去重，重跑不能重复增加 release。
2. **已删除的重复翻译**：针对确切路径 `luci-app-daede/po/zh_Hans/daede.po` 记录继续删除的策略；检查 `zh-cn` 翻译及打包引用仍完整。上游若改为依赖 `zh_Hans` 则停止，不能扩大成所有翻译文件一律删除。

`.github/workflows/release.yml` 本轮虽然文本自动合并成功，仍需检查 x86_64 范围、SDK 24.10/25.12 矩阵、必需包校验和发布目标。无冲突不等于行为一致。

## 4. 自动化流程

```text
定时检测上游 → 锁定 base SHA / upstream SHA
  → 从 main 创建候选 → 三方合并 → 应用有限的确定性规则
  → 更新同一个同步 PR → 必需检查全部通过
  → 自动 merge commit → 记录来源与产物 → 独立发布流程
```

建议每 6 小时检测一次，并提供手动触发入口。排程可能延迟，不承诺实时。

1. 上游 SHA 已是 main 祖先则直接退出。校验上游历史未异常改写；force-push 或来源改变停止处理。
2. 固定本次两个 SHA，生成候选。未知冲突时不提交冲突标记，上传诊断；已有 PR 标记阻塞，新候选无法提交时保留失败运行报告。按 SHA 组合去重通知，不每次排程新建任务。
3. 无冲突或仅命中明确规则时，创建/更新唯一 PR，列出上游提交、受影响文件、应用的规则和验证结果。需要重建机器人候选分支时只允许精确的 `--force-with-lease`，永不强推 main。
4. 用仓库范围的 GitHub App 安装令牌创建分支/PR，使 PR 检查正常触发；默认 `GITHUB_TOKEN` 的事件抑制不能当作检查已经执行。若不使用 App，需设计显式 dispatch 与 SHA 对应的检查回报。
5. main 设置 required checks、合并前必须基于最新 main、禁止强推且不给机器人绕过权限。同步 PR 允许检查通过后自动合并；若对所有 PR 强制人工审批，则正常同步也无法零人工，规则应明确区分。
6. main 在构建期间变化，就重建候选并重新检查。最终检查绑定最新 base 和候选组合，不能复用旧候选的成功结果。同步任务串行，失败/取消/超时/跳过/缺少任意必需检查均不得合并。

同步控制器与验证流程来自受信任的本仓库版本；候选代码只在无发布密钥、无写令牌的隔离构建作业中执行。待合入的 workflow、脚本和策略变化不能自行修改本次验收门槛。首次接入及未知的工作流/权限变更需要审查，已知参数化变更才允许自动通过；不能通过 `pull_request_target` 携带密钥执行候选代码。

## 5. 必需验证

| 门槛 | 检查内容 |
| --- | --- |
| 源码检查 | `git diff --check`；shell 语法；工作流校验；UCI/ACL/菜单和打包引用完整性；下游策略未被覆盖 |
| 现有回归 | `node --test tests/*.test.cjs`、`python3 tests/member-sync-test.py`、`python3 tests/generator-lan-test.py` |
| 定制功能 | 登录/退出、同步周期保存、LAN 选择、配置校验失败保留旧配置、服务重启失败回滚、敏感字段不泄露 |
| 编译 | 默认保持现有产品发布范围：x86_64 × SDK 24.10/25.12；构建 dae、daed、luci-app-daede、vmlinux-btf，并逐项确认产物存在、包元数据正确。扩展架构另立范围 |
| UI / 运行 | 新增真实浏览器窄屏布局检查；网络/eBPF/服务运行依赖测试路由器或专用运行环境。尚无设备自动化时，不宣称已证明实际路由行为 |

复用现有构建能力，但应抽出不上传 feed、不发布 Release、不推 main 的验证入口。不能直接把现有 `release.yml` 当 PR 验证，也不能仅凭作业绿色忽略缺包。现有 Node 测试使用模拟 DOM，不能替代真实 LuCI 主题验证。

## 6. 构建资产、发布与回滚

- 第一期接受上游成套 pins/补丁/源码哈希，下载锁定 tarball 并验证 `PKG_HASH`。为每个已发布版本把确切源码归档到本仓库或受控存储，记录 SHA、哈希与 URL；上游历史资产保留策略不能代替自己的恢复能力。
- 当前流程含写入 `kenzok8/dae`、`kenzok8/outbound`、`kenzok8/quic-go` 的操作和 `FORK_SYNC_TOKEN`。迁移前去掉这些外仓写入依赖；若以后独立装配，自有镜像、上传位置与 Makefile 下载地址必须同时配置并验证。
- 自动合并与正式发布分开。建议先自动合代码、生成候选包，稳定后再为已覆盖运行测试的更新类别开启正式发布。代码合并不会自动升级用户路由器。
- 每个发布版本记录 upstream SHA、下游 SHA、依赖 pins、SDK、包版本和产物哈希；保存上一稳定版包、源码和恢复说明。不要沿用会删除仍被版本引用源码的清理逻辑。
- 出错先停用同步/发布入口。代码通过回滚 PR 恢复；回滚 merge commit 需选择正确主线父提交，并注意 Git 会记住上游已合入，后续修复需显式重新引入被撤销改动。不能假设再同步同一上游 SHA 会自动恢复。
- 路由器恢复上一稳定包并恢复匹配的 UCI/数据库备份；已发生的数据迁移先验证逆向兼容。回退旧代码重新发包时必须使用比故障版更高的包版本，避免设备更新被版本排序挡住。

## 7. 落地顺序与验收

1. **建立基线**：在独立 PR 合入目前 8 个上游提交，处理两个已知冲突，核查工作流语义及新核心补丁；完整测试、构建、代码审查通过后记录稳定基线。
2. **建立自动通道**：新增 `sync-upstream.yml`、无发布副作用的验证工作流、同步脚本和策略文件；改造/禁用旧直推流程；由仓库管理员配置 App、auto-merge 与 main 规则。
3. **故障演练**：验证无更新不产生变更、同一更新不重复 PR、已知冲突可解、未知冲突停止、测试失败不合并、缺包不合并、main 并发变化重新验证、上游 workflow 不能降低检查门槛、回滚产物可恢复。至少覆盖一次正常更新和一次失败案例后开启排程。
4. **渐进减少冲突**：根据实际冲突频率提取定制接入点；需要核心提前更新或自动发布时，再扩展通道，不一开始叠加多套更新源。

本轮交付为方案与只读核查证据：未修改产品代码、未推送、未启用 Actions 或修改仓库设置；未运行本次 SDK 构建、设备验证或新的业务回归。上述测试是实施后的验收要求，不是本轮已通过结果。后续从这里列出的两个 main SHA 重新核对差异即可续接；实施前需复核远端版本和权限配置。
